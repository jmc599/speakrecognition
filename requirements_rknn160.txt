setuptools<82
numpy<2

# official base deps
protobuf==3.20.3
psutil>=5.9.0
ruamel.yaml>=0.17.21
scipy>=1.9.3
tqdm>=4.64.1
opencv-python>=4.5.5.64
fast-histogram>=0.11

# official ONNX toolchain
onnx==1.14.1
onnxoptimizer==0.3.8
onnxruntime==1.16.0

# Deliberately omitted from this env:
# torch==1.13.1
# tensorflow==2.8.0
# We convert from an already-exported ONNX model, so the heavyweight
# frontends are not required unless the toolkit import explicitly asks for them.
